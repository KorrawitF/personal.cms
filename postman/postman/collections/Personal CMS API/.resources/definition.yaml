$kind: collection
description: CRUD endpoints for Project, WorkExperiences, the SkillDomains aggregate (SkillDomains -> Skills -> Experiences), and Content.
variables:
  baseUrl: http://localhost:8080
