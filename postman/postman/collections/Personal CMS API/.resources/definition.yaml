$kind: collection
description: CRUD endpoints for Project, WorkExperiences, the SkillDomains aggregate (SkillDomains -> Skills -> Experiences), Content, and ContactMethod.
variables:
  baseUrl: http://localhost:8080
